
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_broker?createDatabaseIfNotExist=true
    username: postgres
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

