spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:cloud-db
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  cache:
    type: simple


cloud-api:
  integration:
    service: db